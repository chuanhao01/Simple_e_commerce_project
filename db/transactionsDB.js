const transactionsDB = {
    init(pool){
        this.pool = pool;
    },
    // addATransaction(buyer_id){
    //     return new Promise((resolve, reject) => {
    //         this.pool.query(`
    //         `);
    //     });
    // },
};

module.exports = transactionsDB;